extends ../layout/layout.pug

block content
  section.login-popup
      h2.login-popup__title Авторизація
      form.login-popup__form(method='POST')
        label.login-popup__label Логін
          input(
            type='text'
            name='login'
          )
        label.login-popup__label Пароль
          input(
            type='password'
            name='pass'
          )
        label.login-popup__remember
          input(
            type='checkbox'
            name='remember'
          )
          | Запам`ятати мене
        button.btn-submit.login-popup__submit(type='submit') Увійти
        a.login-popup__register(href='/register') Зареєструватися